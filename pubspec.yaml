name: convex_bottom_app_bar
description: customisable bottom app bar
version: 1.0.3
homepage: https://github.com/dwikyhardi
repository: https://github.com/dwikyhardi/convex_bottom_app_bar
issue_tracker: https://github.com/dwikyhardi/convex_bottom_app_bar/issues
documentation: https://github.com/dwikyhardi/convex_bottom_app_bar/blob/master/README.md

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.2"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' and Android 'package' identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.